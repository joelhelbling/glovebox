name: emacs
description: Emacs - the extensible, customizable, self-documenting real-time display editor
category: editor
requires:
  - tools/homebrew
provides:
  - emacs

run_as_user: |
  brew install emacs

env:
  EDITOR: emacs
