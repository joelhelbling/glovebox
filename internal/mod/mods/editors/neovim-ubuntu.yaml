name: neovim-ubuntu
description: Neovim - hyperextensible Vim-based text editor (Ubuntu)
category: editor
requires:
  - ubuntu
provides:
  - neovim

run_as_root: |
  apt-get update && apt-get install -y neovim && rm -rf /var/lib/apt/lists/*

env:
  EDITOR: nvim
