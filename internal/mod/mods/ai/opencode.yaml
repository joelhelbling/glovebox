name: opencode
description: OpenCode AI coding assistant
category: ai
requires:
  - base
provides:
  - opencode

run_as_user: |
  touch ~/.bashrc ~/.zshrc
  curl -fsSL https://opencode.ai/install | bash
