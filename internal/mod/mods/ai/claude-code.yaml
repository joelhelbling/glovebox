name: claude-code
description: Anthropic's Claude Code CLI assistant
category: ai
requires:
  - base
  - bash
provides:
  - claude-code

run_as_user: |
  curl -fsSL https://claude.ai/install.sh | bash
