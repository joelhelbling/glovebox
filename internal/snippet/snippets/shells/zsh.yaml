name: zsh
description: Z shell with sensible defaults
category: shell
requires:
  - base

apt_packages:
  - zsh

run_as_root: |
  usermod -s /usr/bin/zsh ubuntu
  mkdir -p /home/ubuntu/.config/zsh
  chown -R ubuntu:ubuntu /home/ubuntu/.config/zsh

env:
  SHELL: /usr/bin/zsh

user_shell: /usr/bin/zsh
