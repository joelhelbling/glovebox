name: gemini-cli
description: Google's Gemini CLI assistant
category: ai
requires:
  - base
  - languages/nodejs

run_as_root: |
  npm install -g @google/gemini-cli
  mkdir -p /home/ubuntu/.config/gemini
  chown -R ubuntu:ubuntu /home/ubuntu/.config/gemini
