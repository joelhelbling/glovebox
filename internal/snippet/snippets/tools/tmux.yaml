name: tmux
description: Terminal multiplexer with tmuxp session manager
category: tool
requires:
  - base

apt_packages:
  - tmux
  - python3
  - python3-pip
  - pipx

run_as_root: |
  pipx install tmuxp
  mkdir -p /home/ubuntu/.local/bin
  chown -R ubuntu:ubuntu /home/ubuntu/.local
